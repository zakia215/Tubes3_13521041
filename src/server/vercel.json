{
    "version": 2,
    "builds" : [
        {
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/chat",
            "dest": "/api/chat"
        },
        {
            "src": "/api/chat/:id",
            "dest": "/api/chat/:id"
        }
    ]
}